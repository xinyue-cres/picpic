CLIP stage is the sole network path in the entire product — HuggingFace model weights download on first run. It's also the only stage that can OOM on GPU-poor machines; runner catches RuntimeError containing 'out of memory' and halves the batch size in a retry loop. Per-image decode errors during CLIP run are logged to stderr and skipped so a single bad file doesn't kill the pass.
